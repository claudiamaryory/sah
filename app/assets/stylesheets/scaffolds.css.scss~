body {
  background-color:#F0F8FF; /*de aca cambio el color de fondo de la pagina*/
  color: #333;/*de aca cambio el color de la letra*/
  font-family: verdana, arial, helvetica, sans-serif;
  font-size: 13px;
  line-height: 18px;/*de aca se muebe  el contenido del cuerpo abajo o arriba*/
}

p, ol, ul, td {
  font-family: verdana, arial, helvetica, sans-serif;/*de aca cambio el tipo de letra*/
  font-size: 10px;/*de aca cambio el tamaño del menu*/
  line-height: 18px;
}

pre {
  background-color: #eee;
  padding: 10px;
  font-size: 11px;
}

a {
  color: #000;/*de aca cambio el color de los botones*/
  &:visited {
    color: #666;/*de aca cambio el color de los botones*/
  }
  &:hover {
    color: #fff;
    background-color: #000;
  }
}

div {
  &.field, &.actions {
    margin-bottom: 0px;
  }
}

#notice {
  color: green;
}

.field_with_errors {
  padding: 2px;
  background-color: red;
  display: table;
}

#error_explanation {
  width: 450px;
  border: 2px solid red;
  padding: 7px;
  padding-bottom: 0;
  margin-bottom: 20px;
  background-color: #f0f0f0;
  h2 {
    text-align: left;
    font-weight: bold;
    padding: 5px 5px 5px 15px;
    font-size: 12px;
    margin: -7px;
    margin-bottom: 0px;
    background-color: #c00;
    color: #fff;
  }
  ul li {
    font-size: 12px;
    list-style: square;
  }
}
